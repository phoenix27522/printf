#include "main.h"
/**
 * fsp_binary - converts posetive num to binary
 * @args: argument
 *
 * Return: int
 */

int fsp_binary(va_list args, int num)
{
	int i;

	for i(i = num; num / 2 <= 2; num / 2)
	{
		result = num % 2;
	}

	reverse (result);

	return (result);
}
